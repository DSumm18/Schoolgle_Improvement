{
  "name": "@schoolgle/ed-extension",
  "version": "0.1.0",
  "description": "Ed Browser Extension - AI assistant for school tools",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.js --watch",
    "build": "node build.js",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "@types/node": "^20.10.0",
    "esbuild": "^0.20.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "preact": "^10.19.0"
  }
}
