# Fictional Alex practice examples

Captured 7 September 2026 by automated browser QA using the actual game interfaces in isolated browser contexts. All three exports explicitly identify the fictional learner. No real pupil records are present. These are read-only examples, not evidence that a child learned or that the product improves attainment.

- `nile-alex.json`: exact copy of `test-results/fictional-pupil/nile/practice-record.json`; all seven discoveries, Sphinx physical sharing/model/typed calculation and return practice. Final game total 225 gems (the export records completions/bonus rather than an attainment score).
- `plot-alex.json`: exact copy of `test-results/fictional-pupil/plot/casebook.json`; all five chapters plus separate return practice, 100 gems. Original content version and elapsed labels preserved as captured.
- `fire-alex.json`: exact copy of `test-results/fire/alex-casebook.json`; five chapters and final/return recall, including deliberately interrupted/restarted checks, 100 gems.

The example viewer reads these fixtures without writing to localStorage. Its display projection gives readable labels; the JSON download retains original exact responses, models, support and timestamps. Updating them requires another explicit fictional test run, not manually inventing a successful pupil history.
